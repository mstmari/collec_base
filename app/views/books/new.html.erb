<% binding.pry %>
<%= form_for @book do |f| %>
<%= f.label "Price" %>
<%= f.text_field :price %>
<%= f.label "Description" %>
<%= f.text_area :description %>
<%= f.label "Condition" %>
<%= f.text_area :condition %>
<%= f.collection_select :books, Book.all, :id, :name %>
<%= f.submit %>
<% end %>

<%= form_for @book do |f| %>

  <%= render :partial => 'form', :locals=> {:f=>f} %>
<br>
<%= f.submit 'Add Book' %>

<% end %>
